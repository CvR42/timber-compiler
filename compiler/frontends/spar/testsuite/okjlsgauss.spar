// After JLS2 10.5    Arrays: A Simple Example

public class okjlsgauss {
    public static void main() {
        int[] ia = new int[101];
        for (int i = 0; i < ia.length; i++)
            ia[i] = i;
        int sum = 0;
        for (int i = 0; i < ia.length; i++)
            sum += ia[i];
        __println( 1, sum );
    }
}
