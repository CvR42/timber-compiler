// The standard factorial test.

package spar.testsuite;

public class okfac {
    static int fac( int n )
    {
	if( n<2 ){
	    return 1;
	}
	return n*fac( n-1 );
    }
    public static void main()
    {
	for( int n=0; n<10; n++ ){
	    __println( 1, "fac(", n, ")=", fac( n ) );
	}
    }
}
