// bclus-no.sv -- test file for OwnerAbsorption rule.
//   *None* of the foralls gthis program should be
//   transformed into vnus_bclus loops.

package spar.testsuite;


public class okinit
{
    int x = 1;
    static int y = 2;
    
    void performTest()
        {
            __println( 1, x );
            __println( 1, y );

        }
    
    static void main()
        {
            okinit bcn = new okinit();
            
            bcn.performTest();
        }
}
