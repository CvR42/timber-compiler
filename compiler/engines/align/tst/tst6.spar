public class tst6 {
  static int max(int a, int b) {
     return (a > b) ? a : b;
  }

  public static void main() {
    int [*]<$ on = (lambda (i) dsp2D[i]) $> a;

    foreach [i :- 0:10]
      a[i] = a[i] + max(a[i+1],a[i+2]);
  }
};
